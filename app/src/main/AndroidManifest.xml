<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Local music: old Android used a single storage permission. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 13+ granular audio permission. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Optional: some 4.x devices only expose music after a media-store scan. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".CacaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="false">

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SongsActivity" android:label="@string/songs" android:exported="false" />
        <activity android:name=".ui.AlbumsActivity" android:label="@string/albums" android:exported="false" />
        <activity android:name=".ui.AlbumDetailActivity" android:exported="false" />
        <activity android:name=".ui.ArtistsActivity" android:label="@string/artists" android:exported="false" />
        <activity android:name=".ui.ArtistDetailActivity" android:exported="false" />
        <activity android:name=".ui.PlaylistsActivity" android:label="@string/playlists" android:exported="false" />
        <activity android:name=".ui.PlaylistDetailActivity" android:exported="false" />
        <activity android:name=".ui.FavoritesActivity" android:label="@string/favorites" android:exported="false" />
        <activity android:name=".ui.RecentActivity" android:label="@string/recently_played" android:exported="false" />
        <activity android:name=".ui.SearchActivity" android:label="@string/search" android:exported="false" />
        <activity
            android:name=".ui.PlayerActivity"
            android:label="@string/now_playing"
            android:launchMode="singleTask"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.SettingsActivity" android:label="@string/settings" android:exported="false" />
        <activity android:name=".ui.AboutActivity" android:label="@string/about" android:exported="false" />
        <activity android:name=".ui.OnlineActivity" android:label="@string/online_music" android:exported="false" />
        <activity android:name=".ui.OnlineSearchActivity" android:label="@string/online_search" android:exported="false" />
        <activity android:name=".ui.AccountActivity" android:label="@string/account" android:exported="false" />
        <activity android:name=".ui.ThemePickerActivity" android:label="@string/theme" android:exported="false" />

        <service
            android:name=".playback.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <receiver
            android:name=".playback.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".playback.NoisyAudioReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
